{
    "name": "@mashroom/mashroom",
    "description": "Mashroom Server. Supports out of the box the following plugin types: 'web-app', 'api', 'middleware', 'static', 'services' and 'plugin-loader'.",
    "homepage": "https://www.mashroom-server.com",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/nonblocking/mashroom.git"
    },
    "license": "MIT",
    "version": "3.0.0-alpha.0",
    "main": "dist",
    "files": [
        "dist/**",
        "type-definitions/*"
    ],
    "engines": {
        "node": ">=22"
    },
    "bin": {
        "mashroom": "dist/server.js"
    },
    "dependencies": {
        "@mashroom/mashroom-utils": "3.0.0-alpha.0",
        "@types/express": "^5.0.6",
        "anymatch": "^3.1.3",
        "chokidar": "^5.0.0",
        "express": "^5.2.1",
        "fs-extra": "^11.3.6",
        "log4js": "^6.9.1",
        "lucy-dirsum": "^0.1.1",
        "minimist": "^1.2.8",
        "ts-blank-space": "^0.9.0"
    },
    "devDependencies": {
        "@types/fs-extra": "^11.0.4"
    },
    "jest": {
        "testEnvironment": "node",
        "roots": [
            "<rootDir>/test"
        ],
        "testRegex": "(\\.(test|spec))\\.ts$",
        "transform": {
            "^.+\\.ts$": "ts-jest",
            "^.+(?!(chokidar|readdirp)).+\\.js$": "ts-jest"
        },
        "transformIgnorePatterns": ["/node_modules/(?!(chokidar|readdirp)/)"],
        "reporters": [
            "default",
            "jest-junit"
        ]
    },
    "jest-junit": {
        "outputDirectory": "./test-reports"
    },
    "scripts": {
        "lint": "eslint --fix",
        "type-check": "tsc --noEmit",
        "test": "jest --detectOpenHandles",
        "build": "babel src -d dist --extensions \".ts\" && cpy ./src/hooks dist/hooks --flat",
        "dev": "npm run build && node dist/server.js"
    }
}
