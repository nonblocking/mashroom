import type {MashroomRemotePackageScanner} from '@mashroom/mashroom-json-schemas/type-definitions';

const remotePackageUrls: MashroomRemotePackageScanner = {
    remotePackageUrls: [
        'https://demo-ssr-remote-app.mashroom-server.com'
    ]
};

export default remotePackageUrls;
