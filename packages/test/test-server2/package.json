{
    "name": "@mashroom/mashroom-test-server2",
    "version": "1.8.3",
    "private": true,
    "dependencies": {
        "@mashroom/mashroom": "1.8.3"
    },
    "devDependencies": {
        "@mashroom/mashroom-json-schemas": "1.8.3",
        "@babel/cli": "^7.15.7",
        "nodemon": "^2.0.12"
    },
    "scripts": {
        "start": "mashroom",
        "start-cluster": "pm2 start mashroom -i max && pm2 logs",
        "stop-cluster": "pm2 stop mashroom"
    }
}
