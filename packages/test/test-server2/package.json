{
    "name": "@mashroom/mashroom-test-server2",
    "version": "2.4.5",
    "private": true,
    "dependencies": {
        "pm2": "^5.3.0",
        "@opentelemetry/exporter-prometheus": "^0.46.0"
    },
    "devDependencies": {
        "@mashroom/mashroom-json-schemas": "2.4.5"
    },
    "scripts": {
        "start": "mashroom",
        "start-cluster": "pm2 start pm2.json && pm2 logs",
        "stop-cluster": "pm2 stop pm2.json"
    }
}
