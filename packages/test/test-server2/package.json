{
    "name": "@mashroom/mashroom-test-server2",
    "version": "1.7.10",
    "private": true,
    "dependencies": {
        "@mashroom/mashroom": "1.7.10"
    },
    "devDependencies": {
        "@mashroom/mashroom-json-schemas": "1.0.0",
        "@babel/cli": "^7.14.8",
        "nodemon": "^2.0.12"
    },
    "scripts": {
        "start": "mashroom",
        "start-cluster": "pm2 start mashroom -i max && pm2 logs",
        "stop-cluster": "pm2 stop mashroom"
    }
}
