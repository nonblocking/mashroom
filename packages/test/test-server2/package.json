{
    "name": "@mashroom/mashroom-test-server2",
    "version": "1.9.2",
    "private": true,
    "dependencies": {
        "@mashroom/mashroom": "1.9.2"
    },
    "devDependencies": {
        "@mashroom/mashroom-json-schemas": "1.9.2"
    },
    "scripts": {
        "start": "mashroom",
        "start-cluster": "pm2 start mashroom -i max && pm2 logs",
        "stop-cluster": "pm2 stop mashroom"
    }
}
