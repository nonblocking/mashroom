{
    "name": "@mashroom/mashroom-test-server2",
    "version": "1.9.1",
    "private": true,
    "dependencies": {
        "@mashroom/mashroom": "1.9.1"
    },
    "devDependencies": {
        "@mashroom/mashroom-json-schemas": "1.9.1",
        "@babel/cli": "^7.16.0",
        "nodemon": "^2.0.15"
    },
    "scripts": {
        "start": "mashroom",
        "start-cluster": "pm2 start mashroom -i max && pm2 logs",
        "stop-cluster": "pm2 stop mashroom"
    }
}
