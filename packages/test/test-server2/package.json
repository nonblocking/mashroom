{
    "name": "@mashroom/mashroom-test-server2",
    "version": "1.6.0",
    "private": true,
    "dependencies": {
        "@mashroom/mashroom": "1.6.0"
    },
    "devDependencies": {
        "@babel/cli": "^7.12.1",
        "nodemon": "^2.0.6"
    },
    "scripts": {
        "start": "mashroom",
        "start-cluster": "pm2 start mashroom -i max && pm2 logs",
        "stop-cluster": "pm2 stop mashroom"
    }
}
