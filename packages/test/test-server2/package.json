{
    "name": "@mashroom/mashroom-test-server2",
    "version": "2.0.7",
    "private": true,
    "dependencies": {
        "@mashroom/mashroom": "2.0.7"
    },
    "devDependencies": {
        "@mashroom/mashroom-json-schemas": "2.0.7"
    },
    "scripts": {
        "start": "mashroom",
        "start-cluster": "pm2 start ./starter.js --name mashroom -i max && pm2 logs",
        "stop-cluster": "pm2 stop mashroom"
    }
}
