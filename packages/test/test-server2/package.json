{
    "name": "@mashroom/mashroom-test-server2",
    "version": "1.8.0",
    "private": true,
    "dependencies": {
        "@mashroom/mashroom": "1.8.0"
    },
    "devDependencies": {
        "@babel/cli": "^7.14.8",
        "@mashroom/mashroom-json-schemas": "1.8.0",
        "nodemon": "^2.0.12"
    },
    "scripts": {
        "start": "mashroom",
        "start-cluster": "pm2 start mashroom -i max && pm2 logs",
        "stop-cluster": "pm2 stop mashroom"
    }
}
