apiVersion: v1
kind: Service
metadata:
    name: mashroom-portal
    namespace: portal
    labels:
        app: mashroom-portal
spec:
    selector:
        app: mashroom-portal
    ports:
        -   port: 5050
            targetPort: 5050
