apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: mashroom-portal-sessions-pvc
    namespace: portal
spec:
    accessModes:
        - ReadWriteOnce
    storageClassName: local-path
    resources:
        requests:
            storage: 1Gi
