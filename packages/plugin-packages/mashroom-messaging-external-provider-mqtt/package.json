{
    "name": "@mashroom/mashroom-messaging-external-provider-mqtt",
    "description": "Allows to use a MQTT server as external messaging provider for service side messaging",
    "homepage": "https://www.mashroom-server.com",
    "repository": "github:nonblocking/mashroom",
    "license": "MIT",
    "version": "1.2.3",
    "files": [
        "dist/**"
    ],
    "dependencies": {
        "@mashroom/mashroom-utils": "1.2.3",
        "mqtt": "^2.18.8"
    },
    "devDependencies": {
        "@babel/cli": "^7.6.4",
        "@mashroom/mashroom": "1.2.3",
        "@mashroom/mashroom-messaging": "1.2.3",
        "eslint": "^6.6.0",
        "express": "^4.17.1",
        "jest": "^24.9.0"
    },
    "jest": {
        "verbose": true,
        "testEnvironment": "node",
        "roots": [
            "<rootDir>/test"
        ],
        "testRegex": "(\\.(test|spec))\\.js$"
    },
    "scripts": {
        "lint": "eslint src test --fix",
        "test": "jest --passWithNoTests",
        "build": "babel src -d dist"
    },
    "mashroom": {
        "devModeBuildScript": "build",
        "plugins": [
            {
                "name": "Mashroom Messaging External Provider MQTT",
                "type": "external-messaging-provider",
                "bootstrap": "./dist/mashroom-bootstrap.js",
                "requires": [
                    "Mashroom Messaging Services"
                ],
                "defaultConfig": {
                    "internalTopic": "mashroom",
                    "mqttConnectUrl": "mqtt://localhost:1883",
                    "mqttProtocolVersion": 4,
                    "mqttQoS": 1,
                    "mqttUser": null,
                    "mqttPassword": null,
                    "rejectUnauthorized": true
                }
            }
        ]
    }
}
