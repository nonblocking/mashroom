import React from 'react';
import {DialogContent} from '@mashroom/mashroom-portal-ui-commons';
import Permissions from './Permissions';

export default () => (
    <DialogContent>
        <Permissions />
    </DialogContent>
);
