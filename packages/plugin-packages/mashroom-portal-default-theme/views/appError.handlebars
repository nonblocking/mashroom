{{! This template is optional, since it resembles the default  }}
<div class="mashroom-portal-app-loading-error">
    {{#if errorMessage}}
        {{errorMessage}}
    {{else}}
        {{{defaultPluginErrorMessage title messages}}}
    {{/if}}
</div>
